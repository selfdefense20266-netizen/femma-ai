// project import
import dashboard, { account } from './dashboard';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, account]
};

export default menuItems;
